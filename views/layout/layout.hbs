
    {{#if isAdmin}}
    {{>admin-header}}
    {{>admin-nav}}
    {{else if isUser}}
    {{>user-header}}
    {{/if}}

    {{{body}}}

    {{#if isAdmin}}
    {{>admin-footer}}
    {{else if isUser}}
    {{>user-footer}}
    {{/if}}

 {{!-- {{#if isAdmin}}
    {{>admin-header}}
    {{>admin-nav}}
    {{/if}}

    {{#if isUser}}
    {{>user-header}}
    {{/if}}

    {{{body}}}

    {{#if isAdmin}}
    {{>admin-footer}}
    {{/if}}

    {{#if isUser}}
    {{>user-footer}}
    {{/if}}
     --}}
    

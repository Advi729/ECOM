<section class="content-main justify-content-center">
 
    {{#if validationError}}
    {{#each validationError}}
    <div class="alert alert-danger product-validation-alert">
        {{this}}
    </div>
    {{/each}}
    {{!-- <p style="color: red;">{{validationError}}</p><br> --}}
    {{/if}}
            <form id="myForm" action="/admin/add-banner" method="POST" enctype="multipart/form-data">
            <div class="row">
              {{!-- <form action="/admin/add-product" method="POST" enctype="multipart/form-data"> --}}
                <div class="col-9">
                    <div class="content-header">
                        <h2 class="content-title">Add Banner</h2>
                        <div>
                        <button class="btn btn-md rounded font-sm hover-up" type="submit">Publish</button>
                        </div>
                        {{!-- <div>
                            <button class="btn btn-light rounded font-sm mr-5 text-body hover-up">Save to draft</button>
                            <button class="btn btn-md rounded font-sm hover-up">Publich</button>
                        </div> --}}
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Basic details</h4>
                        </div>
                        <div class="card-body">
                            {{!-- <form action="/admin/add-product" method="POST" enctype="multipart/form-data"> --}}
                                <div class="mb-4">
                                    <label for="title" class="form-label">Name</label>
                                    <input type="text" placeholder="Type here" name="name" class="form-control" id="title">
                                </div>

                                <div class="mb-4">
                                    <label for="title" class="form-label">Heading</label>
                                    <input type="text" placeholder="Type here" name="heading" class="form-control" id="title">
                                </div>

                                <div class="mb-4">
                                    <label for="title" class="form-label">Sub heading</label>
                                    <input type="text" placeholder="Type here" name="subHeading" class="form-control" id="title">
                                </div>

                                <div class="mb-4">
                                    <label for="title" class="form-label">Footer</label>
                                    <input type="text" placeholder="Type here" name="footer" class="form-control" id="title">
                                </div>
                                
                            {{!-- </form> --}}
                        </div>
                    </div> <!-- card end// -->

                </div>
                <div class="col-lg-3">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Media</h4>
                        </div>
                        <div class="card-body">
                            <div class="input-upload">
                                <img src="/assets-admin/imgs/theme/upload.svg" alt="">
                                <input required="" type="file" name="image" single="" class="form-control">
                            </div>
                        </div>
                    </div> <!-- card end// -->
                     
                </div>


<div class="card mb-4 col-md-6">

  <header class="card-header">
    <div class="row align-items-center">
      

        <div class="form-group mr-5 align-items-center" style="float: left;">
            <h4>All Banners</h4>
        </div>

        
    
    </div>
    
  </header>
</div> 
<div class="card" id="sales-report">
                <div class="card-body">
                    <div class="row">

                {{!-- <div class="col-md-9"> --}}
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                          
                                            <th>Sl no.</th>
                                            <th>Name</th>
                                            <th>Heading</th>
                                            <th>Sub Heading</th>
                                            <th>Footer</th>
                                            <th>Edit</th>
                                            <th>Delete</th>
                                            
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each allBanners}}
                                        <tr>
                                            <td>{{index @index}}.</td>
                                            <td><b>{{this.name}}</b></td>
                                            <td>{{this.heading}}</td>
                                            <td>{{this.subHeading}}</td>
                                            <td>{{this.footer}}</td>
                                            {{!-- <td>
                                                <div class="dropdown">
                                                    <a href="#" data-bs-toggle="dropdown" class="btn btn-light rounded btn-sm font-sm"> <i class="material-icons md-more_horiz"></i> </a>
                                                    <div class="dropdown-menu">
                                                        <ul>
                                                            {{#each subCategory}}
                                                            <li>{{this}}</li>
                                                            {{/each}}
                                                        </ul>
                                                    </div>
                                                </div> <!-- dropdown //end -->
                                            </td> --}}
                                            <td>
                                                <a href="/admin/edit-banner/{{this._id}}" onclick="event.preventDefault(); editConfirmation(this)" data-id="{{this._id}}">
                                                    <img src="/assets-admin/imgs/icons/edit.png" width="33em">
                                                </a>
                                            </td>
                                            <td>
                                                
                                                <a href="/admin/delete-banner/{{this._id}}" onclick="event.preventDefault(); deleteConfirmation(this)" data-id="{{this._id}}">
                                                    <img src="/assets-admin/imgs/icons/delete.png" width="33em">
                                                </a>
                                                
                                            </td>
                                            {{!-- <td class="text-end">
                                                <div class="dropdown">
                                                    <a href="#" data-bs-toggle="dropdown" class="btn btn-light rounded btn-sm font-sm"> <i class="material-icons md-more_horiz"></i> </a>
                                                    <div class="dropdown-menu">
                                                        <a class="dropdown-item" href="#">View detail</a>
                                                        <a class="dropdown-item" href="#">Edit info</a>
                                                        <a class="dropdown-item text-danger" href="#">Delete</a>
                                                    </div>
                                                </div> <!-- dropdown //end -->
                                            </td> --}}
                                        </tr>
                                        {{/each}}
                                    </tbody>
                                </table>
                            </div>
                        {{!-- </div> --}}
                    </div>
                </div>
</div>

            </div>
            </form>
        </section> <!-- content-main end// -->


<script>


// swal for adding product
const myForm = document.getElementById('myForm');
myForm.addEventListener('submit', (event) => {
  event.preventDefault(); // prevent the form from submitting normally
  
  Swal.fire({
    icon: 'warning',
    title: 'Are you sure?',
    text: 'Do you want to publish this banner?',
    showCancelButton: true,
    confirmButtonColor: '#3085d6',
    cancelButtonColor: '#d33',
    confirmButtonText: 'Yes'
  }).then((result) => {
    if (result.isConfirmed) {
        Swal.fire({
            title: "Success!",
            text: "Banner added successfully!",
            icon: "success",
            showConfirmButton: false,
        });
      // User confirmed the action, submit the form
      setTimeout(() => {
        myForm.submit();
      }, 2000);
    }
  });
});

// swal to delete a banner
    function deleteConfirmation(anchor) {
        const id = anchor.getAttribute('data-id');
        Swal.fire({
            title: 'Are you sure?',
            text: "Do you want to delete this banner?",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                Swal.fire({
                    title: "Success!",
                    text: "Banner deleted successfully.",
                    icon: "success",
                    showConfirmButton: false,
                });
                // Redirect to the delete URL
                setTimeout(() => {
                    window.location.href = anchor.href;
                },2000);
            }
        });
    }

// swal for editing the banner
    function editConfirmation(anchor) {
        const id = anchor.getAttribute('data-id');
        Swal.fire({
            title: 'Are you sure?',
            text: "Do you want to edit this banner?",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes',
        }).then((result) => {
            if (result.isConfirmed) {
                // Redirect to the unlist URL
                window.location.href = anchor.href;
            }
        });
    }


// Select all the error alerts
const errorAlerts = document.querySelectorAll('.product-validation-alert');
// Loop through the error alerts
errorAlerts.forEach(alert => {
    // Set a timeout of 5 seconds to remove the alert
    setTimeout(() => {
    alert.remove();
    }, 5000);
});

</script>



